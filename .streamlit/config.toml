[server]
# The app can launch simulation processes: only accept connections from this machine.
address = "localhost"
headless = true

[browser]
gatherUsageStats = false

[client]
toolbarMode = "minimal"
